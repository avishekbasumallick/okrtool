# Gemini API key for AI categorization/prioritization/scope+deadline generation
GEMINI_API_KEY=your_api_key_here

# Optional: set a specific model. If omitted, the server will auto-select a model that supports generateContent.
GEMINI_MODEL=
